---
type: concept
tags: [databases, data-structures, indexes, algorithms]
---
# B-tree / B+ tree

Пояснення Віктора «біля дошки», чому індекси баз даних — не бінарні дерева пошуку: BST над 100 млн рядків має глибину ~23 рівні, і при ~5 мс на дисковий seek така глибина фатальна. B-tree виправляють це, підганяючи розмір вузла під дисковий блок (8 КБ у Postgres/MySQL): кожен вузол отримує приблизно 200–2000 дітей, і дерево скорочується до ~4 чи навіть 2 рівнів ([[trees-search-algorithms-databases|дерева й алгоритми пошуку]]). Насправді бази даних використовують B+ tree: дані живуть лише в листках, а листки зчеплені між собою по горизонталі, тож діапазонний скан на кшталт «ціна 100–110» іде ланцюжком листків замість повторного спуску деревом — саме тому за O(log n)-поведінкою, яку він бенчмаркає, стоять B+ tree, а не відсортовані масиви ([[why-algorithms-matter|навіщо алгоритми]]).

Нутрощі важать на практиці. У MySQL сама таблиця І Є B+ tree, відсортованим за первинним ключем (кластерний індекс), — ось чому вставки випадкових UUID термосять кешування вузлів дерева, тоді як послідовні auto-increment-вставки торкаються лише закешованих крайніх правих вузлів ([[database-indexes-mysql-vs-postgres|індекси в MySQL і Postgres]]). Уперше він натякнув на це ще в оригінальному відео про індекси: індекс — це насправді B+ tree, а не простий BST, існують і хеш-індекси, а простий відсортований список теж працював би, але коштував би O(N) на підтримку ([[why-database-indexes|навіщо потрібні індекси]]).

## Де розглядається
- [[trees-search-algorithms-databases]] — вичерпний розбір: глибина BST проти дискових seek-ів, вузли розміром із блок, зчеплені листки B+ для діапазонних сканів
- [[database-indexes-mysql-vs-postgres]] — таблиця MySQL як B+ tree і чому UUID-вставки її термосять
- [[why-database-indexes]] — перша згадка: індекс — це B+ tree; існують хеш-індекси; відсортовані списки коштували б O(N) на підтримку
- [[why-algorithms-matter]] — справжні індекси БД — збалансовані B+ tree, що зберігають O(log n) бінарного пошуку плюс діапазонні скани

## Повʼязане
[[database-indexes]] — те, заради чого B+ tree збудовані
[[data-structures]] — BST, AVL-балансування і вироджені випадки, яких B-tree уникають
[[algorithmic-complexity]] — гарантія log N, заради захисту якої дерево існує
[[uuid-vs-auto-increment]] — порядок вставок проти кешування B+ tree
