---
type: tool
tags: [databases, sql, relational]
---
# MySQL

Стандартний експериментальний полігон каналу: майже всі демо з базами даних крутяться на MySQL — через `SET profiling`, `CREATE INDEX` та `EXPLAIN` ([[why-database-indexes]]). Її визначальна архітектурна риса у викладі Віктора — кластерний індекс: сама таблиця є [[b-tree|B+ tree]], відсортованим за первинним ключем — вона буквально не може існувати без нього (пропустіть PK — і MySQL мовчки додасть прихований 6-байтовий). Звідси все, що він вимірює: пошук за вторинним індексом — це подвійний пошук, а випадкові UUID у ролі первинних ключів роздувають таблицю з 4.3 ГБ до 7.8 ГБ і сповільнюють вставки у 2.5–3 рази на повільних дисках — серцевина вердикту [[uuid-vs-auto-increment|UUID проти auto-increment]] ([[database-indexes-mysql-vs-postgres]]). Плюс у порівнянні з [[postgresql|PostgreSQL]]: за [[mvcc|MVCC]] MySQL оновлює лише той індекс, якого реально торкнулася зміна, а не перебудовує всі.

В арці про повнотекстовий пошук MySQL грає і базову лінію, і суперника: `LIKE '%...%'` займає 40 с на 16.5 млн рядків (а на HDD взагалі відвалюється по таймауту), тоді як `CREATE FULLTEXT INDEX` будується 14 хвилин і займає 1.7 ГБ — більше за його саморобний інвертований індекс на 1.3 ГБ — хоча й підтримує булеві запити must/must-not; всередині InnoDB FULLTEXT розвʼязує проблему UUID-ідентифікаторів документів прихованою auto-increment колонкою FTS_DOC_ID ([[full-text-search-inverted-indexes]], [[full-text-search-part-2-qa]]). У бенчмарках із Bloom-фільтром він зазначає, що точкові пошуки в MySQL майже такі ж швидкі, як кеш, — повільність живе у сканах і джоїнах — і демонструє Handler Socket, прямий доступ до індексу в обхід планувальника запитів (трюк, який він оцінював ще в часи роботи над Порталом), — той виявляється лише трохи швидшим за prepared statements по індексу ([[bloom-filter-and-firefox]]). MySQL — це і його дежурний приклад необхідної глибини: якщо ви не знаєте, як вона обробляє UPDATE чи як поводиться зі зростанням даних, ви не знаєте її властивостей ([[3-things-that-make-a-programmer-better]]).

## Де розглядається
- [[why-database-indexes]] — усі первісні демо з індексами: профілювання, EXPLAIN, 3.3 с → 0.05 мс
- [[database-indexes-mysql-vs-postgres]] — три таблиці по 16 млн рядків, covering-індекси в EXPLAIN, бенчмарки вставок, архітектура кластерного індексу
- [[trees-search-algorithms-databases]] — вузли дерева індексу обмежені розміром блоку, а не кількістю елементів
- [[full-text-search-inverted-indexes]] — базова лінія LIKE проти суперника FULLTEXT (1.7 ГБ, 14 хв побудови)
- [[full-text-search-part-2-qa]] — прихована колонка FTS_DOC_ID в InnoDB; LIKE, що відвалюється по таймауту на HDD
- [[bloom-filter-and-firefox]] — бенчмарки на 10 млн ключів і відступ про Handler Socket
- [[qa-1-will-https-protect-you]] — UUID-ключі як BINARY(16) на практиці
- [[3-things-that-make-a-programmer-better]] — нутрощі MySQL як приклад потрібної глибини

## Повʼязане
[[database-indexes]], [[b-tree]], [[uuid-vs-auto-increment]], [[mvcc]], [[full-text-search]], [[postgresql]]
